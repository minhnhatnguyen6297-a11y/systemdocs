# Progress — MIN-69

## Trạng thái: T1–T9 xong + verify + **đã merge vào `consolidate/monorepo`** — 25/09/2026 (còn T10 launcher cutover ngoài phạm vi)

**Merge record:** merge commit `26f9a3f` trên `consolidate/monorepo`, đã push origin. Union với MIN-104–117 (notary case-drafting, packaging, JobStore fixes) + MIN-92/103 (zalo-intake contract) — 12 conflict resolve union. Vá trong merge: `except CancelledByUser` thiếu `job._notify()` → repository giữ non-terminal → resync sai `failed{engine_restarted}` (test_upload_recovery bắt). Verify merged tree: npm 184/184, pytest shell 276/276, contract validators 0 unexpected. Linear comment 25/09 — giữ In Review, còn owner-gate: duyệt `upload.workflow.v1` (DRAFT), pilot portal thật, chốt KPI issue_count, clean-machine + NSIS, T10 cutover, migrate dữ liệu thật.

Triển khai tại worktree riêng `D:/systemdocs-min-69` (đã gỡ sau merge), nhánh `minhnhatnguyen6297/min-69-migrate-uploadaudit-vao-electron` (base `consolidate/monorepo` @ 1abbb20; đã merge — nhánh local đã xóa). Quy trình: subagent-driven development — mỗi task có implementer + reviewer + fix loop riêng. Owner duyệt contract khi review nhánh trước merge (user đã chọn "code hết T1–T9").

## Tiến độ task (theo kế hoạch)

- **T1 (xong, review sạch):** spec_UI.md + README viết lại theo 2 tab; `contracts/upload-workflow.md` (DRAFT — chờ owner duyệt) + 53 examples + `validate_examples.py` (37 valid pass / 16 invalid reject đúng mã); 17 commands, `workflow_version`, scope/revision guards.
- **T2 (xong, review sạch):** `upload_lab/providers/` (nam_dinh duy nhất, unknown → `unknown_website`); `shell/sidecar/upload_workspace{,_store}.py` (data root qua `G1_UPLOAD_DATA_DIR`, per-website `websites/<id>/`, SQLite store); `upload_lab/tools/migrate_shell_data.py` (inspect/apply, snapshot+hash verify); `shell/test/test_upload_workspace.py` 27 tests.
- **T6 (xong, review sạch):** `shell/src/renderer/upload/` 2 tab (Audit Sổ Công Chứng + Quét & Upload Hồ Sơ), state chung `createUploadState/selectTab/acceptScopedResult`, CSS scoped `.upload-lab` full-width, 31 tests mjs mới (70/70 npm test).
- **T3 (xong, review sạch):** `upload.scan/audit_excel/queue_get` v1 — manifest file của đúng run (binding+sha256, không fallback file mới nhất), audit_id gắn website+khoảng ngày, `classify_scan_records` thật, `_v1_boundary` chuẩn hóa next_action; `test_upload_workflow.py` 21 tests.
- **T4 (xong):** `_BrowserWorker` wait-state theo job + op-slot `browser_busy` + idle poll snapshot + `_session_lost` reconcile; v1 handlers session_start/confirm_login/session_status/session_close/download_export/staff_options/prepare/finish_review/reconcile; `upload.staff_options` + `upload.reconcile` đăng ký mới; `/health` quảng bá `upload.workflow.v1`; `test_upload_browser_workflow.py` 28 tests qua localhost fake portal (27F+1E RED → GREEN).
- **T5 (xong):** Job terminal bất biến (transition dưới lock, cancel thắng race late-success/failure/progress); `job_repository.py` mới — bảng `sidecar_jobs` trong workspace.sqlite3 (command_id UNIQUE + request_hash → `command_id_conflict` 409, không lưu payload); restart materialize non-terminal → `failed{engine_restarted}`, không replay handler, `get(job_id)` resync lazy; `_recover_interrupted` sweep lúc mở store (workflow_jobs→failed, browsers→closed, open_tabs→needs_reconcile giữ run_id); `worker.shutdown(timeout)` + `_poll_then_close` nguyên tử, tab chưa verify → needs_reconcile không bao giờ 'đã Lưu'; adapter mirror `Job.snapshot()` qua `set_job_status` (duy nhất ghi terminal), prepare tách prepared/saved, `needs_reconcile_record_ids` → engine_unavailable non-retryable; `drain(timeout)` cancel engine_shutdown + persist trước khi đóng repo; tracker.js: instance change → engine_restarted cục bộ + `getJob` resync terminal thật, terminal cục bộ bất biến với snapshot trễ. Tests: test_jobstore +18, test_upload_recovery.py mới 14, test_sidecar_contract +409, job-tracker.test.mjs mới 4 — Python 145 ok + npm 74 ok. **Fix1 (review):** `worker.shutdown(timeout)` chia budget reconcile+join theo deadline (không còn ~2·timeout), `SHUTDOWN_GRACE_MS` 3s→6s (bound _stop ~4.5s + headroom), `record_snapshot` terminal guard CASE WHEN (status+snapshot_json); +2 test Python +1 mjs — Python 146 ok + npm 75 ok.
- **T7 (xong, commit cdedafd):** tab Audit nối trọn — catalog-driven selector + env/login/download/pick → auto audit → 4 KPI + 2 bảng MIN-77; once-per-job adopt (audit/download/ws_get/website_select/session_start), revision forward-only (vá stale_revision), session_status checked_at guard, catalog/ws retry qua derive khi sidecar chưa ready; main.js openPath FileRef scope + ext allowlist + seam G1_E2E_PICK_FILES; test_upload_e2e.py --case audit (Electron thật + CDP + fake_portal hook) PASS; npm test 84/84. Bugs vá trong bring-up: latestJob tie ≥, catalog bootstrap một-lần kẹt khi submit sớm, session_start re-adopt kéo revision lui.
- **T8 (xong):** tab Quét & Upload nối trọn — chi tiết `task-8-report.md`.
- **T9 (xong, verify đầy đủ trên máy build):** packaged Windows app — `engine_roots` bundled-dir resolution + `engine_data_dir` writable redirect; `notary_adapter` rebind `DB_PATH`/`engine`/`SessionLocal` khi root read-only; PyInstaller onedir (engine deps, playwright datas, exclude Qt/numpy/pandas); stage script allowlist engine source + Chromium `*-1243` → `build/playwright-browsers/`; `dist` → `dist-app/` (appId `dev.g1.shell`), `dist:test` → `dist-test/` (appId `dev.g1.shell.test`, marker `resources/test-pkg/test-build.json` → `G1_BUILD_LABEL=test` + E2E seams + fixture portals; production seam no-op, không ship `e2e_fixture_hook`); `/healthz` trả `build_label`. E2E harness `--app` + `--case offline|all` có build-label enforcement; `--case offline` PASS trên gói production kèm **ACL deny-write** (mask `WD,AD,WA,WEA,DC`) trên install dir — data/output ghi dưới userData, install dir bất biến, chỉ `nam_dinh`, `diag.fixture_browser_launch` = `command_unknown`, không Qt; `--case all` PASS trên gói test — audit + Chromium bundled launch thật (loopback) + upload/recovery/reconcile. Adapter tests isolate temp (`G1_NOTARY_DATA_DIR`/`G1_UPLOAD_DATA_DIR`/`G1_OUTPUT_DIR`). Suites: npm **108/108**, sidecar contract **21**, jobstore **18**, upload workflow **21**, browser workflow **35**, recovery **16**, adapters **10**, contract validators **10+53 files 0 unexpected**, frozen probe PASS. upload_lab subset 45 pass, 3 Qt-tests không chạy được (thiếu PySide6 trong venv — không phải regression). **Không** portal thật, **không** clean-machine/pilot, **không** NSIS, **không** cutover launcher (T10). Báo cáo: `.superpowers/sdd/2026-09-24-upload-lab-shell-migration-plan/task-9-report.md`.
- **T9 fix1 (review `6dd76cf`):** F1 `legacy_data_dir()` — mọi `upload.*` legacy (không workflow_version) redirect data root sang `<G1_UPLOAD_DATA_DIR>/legacy` khi bundled (scan/env_check/prepare/session — không ghi install dir; dev giữ engine root). F2 spec += `multipart`/`tzdata`; `requirements-package.txt` pin 16 dist theo build venv + `check_package_deps.py` trong cả hai build script. F3 packaged spawn strip `PYTHONPATH/PYTHONHOME/PYTHONSTARTUP` + `_install_e2e_fixtures` cần `G1_BUILD_LABEL=test` ∧ `G1_E2E_FIXTURE=1`; dev harness set label=test và `e2e_fixture_hook.install()` idempotent (vá double-register crash sẵn có của dev E2E). F4 `G1_NOTARY_DATA_DIR=<userData>/engine-data/notary_v2` packaged + `engine_data_dir` fallback ra khỏi `output/`. F5/F6 stage loại `word_templates/custom` + `services/fast_audit` (+ forbidden check). F7 `ocr.analyze`: `_get_api_key()` 0-arg + `_ENV_PATH` về engine-data khi bundled. N1 bỏ `$hook` chết; N4 frozen probe có tree sweep `_internal`/resources; N7 stage sạch `playwright-browsers/` trước copy. Verify lại đầy đủ: npm **110/110**, contract **22**, adapters **13** (+3 packaged-sim F1), jobstore **18**, workspace **27**, workflow **21**, browser **35**, recovery **16**; `build:sidecar` + `dist` + `dist:test` build lại OK; frozen probe PASS (tree sweep sạch); production `--case offline` PASS (ACL deny-write); test `--case all` PASS; dev `--case audit` + `--case upload` PASS. Chưa: contract validators (không đụng contract), portal thật, clean-machine, NSIS, T10. Báo cáo: `task-9-fix1-report.md`.
- **T9 fix2 (re-review `c91b353` nit):** strip `PYTHON_INHERIT_DENYLIST` case-insensitive (Windows env case-insensitive với child — `PythonPath` variant trước đây sống sót vào frozen exe); denylist += `PYTHONUSERBASE` (redirect user site-packages, cùng lớp injection). Test: `envdump` += `pythonuserbase`/`python_keys` (key sót theo case thật, lookup case-insensitive đọc được variant trên *nix); packaged assert case variants + USERBASE = null và `python_keys` rỗng; dev assert variant được giữ. Verify: negative-check test fail đúng trên code cũ; npm **110/110**. Báo cáo: `task-9-fix2-report.md`.
- **Final-review fix (F1–F4 + drift):** F1 `prepare_manifest(prime_save_validation=True)` — v1 adapter truyền `False`, `_fill_dropdown`/`_prime_contract_name_validation` gate không click Lưu "mồi" trên đường v1 (legacy/Qt giữ default). F2 `poll_prepared_pages(strict_save_evidence=False)` + `PreparedBrowserTab.reported_uncertain` — strict: chỉ POST `/api/hoso` 2xx là saved; rời trang tạo không có POST → closed/uncertain một lần → needs_reconcile, tab vẫn tracked để POST đến sau self-heal; `_poll_browser` bật strict khi `_website_id is not None`. F3 `upload.reconcile` đọc `needs_reconcile_rows(wid)` website-wide (kể cả `run_id=None`/stamp run khác), lookup `get_row_by_id` toàn cục, bump queue_revision run payload + mọi run còn stamp trên row verified; `add_needs_reconcile` COALESCE giữ provenance khi update None; `_prepare_result_data` báo needs website-scope. F4 `_ScanAbortRequested(BaseException)` — `CancelledByUser` bị `emit_progress` `except Exception` nuốt → abort BaseException thoát, catch ngoài → `_finish_workflow_job(canceled)` + re-raise `CancelledByUser`. Drift: registry.js `capabilities: ['upload.workflow.v1']`, desktop-command shutdown 3s→6s, upload-workflow.md §6.14/§6.15/§7 (DRAFT giữ nguyên). Tests mới: browser_workflow +9 (prime kwarg, strict gate theo scope, mid-scan cancel, reconcile website-wide, engine strict uncertain-once/self-heal/legacy-keep, fill_dropdown prime gate), workflow +1 (cancel thoát `emit_progress` thật). Verify: Python **149** pass, upload_lab engine **27** pass, npm **110/110**, contract validator **53 files 0 unexpected**. Legacy unversioned `upload.*`/Qt không đổi — mọi gate chỉ trên v1. Chưa: portal thật (validation ten_hop_dong không-submit là NOTE trong contract), clean-machine, NSIS. Báo cáo: `final-review-fix-report.md`.
- **T10:** chưa bắt đầu — launcher/default-entrypoint cutover (ngoài phạm vi T9).

## Đã làm (phiên kế hoạch trước)

- Lập [kế hoạch chuyển đổi](D:/systemdocs/docs/product/plans/2026-09-24-upload-lab-shell-migration-plan.md).
- Đọc chuỗi launcher: bản Fluent UI/Qt hiện tại là giao diện được run.bat mở.
- Đối chiếu source với hai manifest codegraph Upload Lab: bản Fluent có 9/38 tệp khác dấu kiểm tra; bản thử nghiệm Electron có 10/53.
- Tạo brief và [handoff](D:/systemdocs/.agent/tasks/MIN-69/handoff.md).

## Kiểm chứng và giới hạn

- Python test env: `D:/systemdocs/notary_v2/venv/Scripts/python.exe` (shell/sidecar), `D:/systemdocs/upload_lab/.venv/Scripts/python.exe` (upload_lab, có PySide6). Baseline xanh trước khi code.
- Đã đóng gói + verify trên máy build (T9): `dist-app/win-unpacked/g1-shell.exe` production offline PASS (ACL deny-write), `dist-test/win-unpacked/g1-shell-test.exe` full fixture E2E PASS. **Chưa** clean-machine/pilot — gói chỉ mới chạy trên máy build.
- Chưa chạy portal thật (mọi browser flow qua fixture loopback), chưa chuyển dữ liệu thật, chưa cutover launcher — T10 còn lại. Contract vẫn DRAFT chờ owner duyệt.
- Venv không nằm trong worktree (machine-local ở checkout chính) — implementers dùng đường dẫn tuyệt đối ở trên. PyInstaller build dùng `notary_v2` venv (đủ deps: PyInstaller, fitz, playwright); `upload_lab/.venv` KHÔNG dùng được cho build (thiếu PyInstaller/fitz).
